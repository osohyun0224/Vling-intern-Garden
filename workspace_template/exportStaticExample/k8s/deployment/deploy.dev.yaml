apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: svc-dev-vling-mf-search
  annotations:
    # https://github.com/kubernetes-sigs/aws-load-balancer-controller/issues/1056#issuecomment-551585078
    alb.ingress.kubernetes.io/healthcheck-path: /api/healthcheck
    alb.ingress.kubernetes.io/healthcheck-interval-seconds: '30'
  labels:
    app: svc-dev-vling-mf-search
    stage: dev
    deploy: ramped
    tier: frontend
spec:
  type: NodePort
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
  selector:
    app: dev-vling-mf-search
    stage: dev
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: default
  name: dev-vling-mf-search
spec:
  minReadySeconds: 5
  selector:
    matchLabels:
      app: dev-vling-mf-search
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  replicas: 1
  template:
    metadata:
      labels:
        app: dev-vling-mf-search
        stage: dev
        commit: 3a7d66008c4cd91705ba73a5ecc044d5a2493597
        version: v20231227_1703667110
        deploy: template
        tier: frontend
    spec:
      nodeSelector:
        kops.k8s.io/instancegroup: nodesdev
      tolerations:
        - key: dev
          operator: Exists
      terminationGracePeriodSeconds: 30
      containers:
        - name: dev-vling-mf-search
          image: 314916389090.dkr.ecr.ap-northeast-2.amazonaws.com/vling_gui_v2:dev.mf.search
          imagePullPolicy: 'Always'
          ports:
            - containerPort: 4000
          resources:
            limits:
              cpu: '200m'
              memory: '400Mi'
            requests:
              cpu: '200m'
              memory: '300Mi'
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /api/readiness
              port: 4000
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 10
            initialDelaySeconds: 30
          livenessProbe:
            httpGet:
              path: /api/healthcheck
              port: 4000
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 10
            initialDelaySeconds: 30
